{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "VendorOnboarded",
  "type": "object",
  "properties": {
    "event_id": { "type": "string", "format": "uuid" },
    "vendor_id": { "type": "string" },
    "onboarded_at": { "type": "string", "format": "date-time" },
    "source": { "type": "string" }
  },
  "required": ["event_id", "vendor_id", "onboarded_at"]
}
