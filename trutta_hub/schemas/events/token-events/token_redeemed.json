{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TokenRedeemed",
  "type": "object",
  "properties": {
    "event_id": { "type": "string", "format": "uuid" },
    "token_id": { "type": "string" },
    "redeemed_at": { "type": "string", "format": "date-time" },
    "source": { "type": "string" }
  },
  "required": ["event_id", "token_id", "redeemed_at"]
}
